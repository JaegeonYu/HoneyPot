export interface BillProps {
  partycolor: string;
  partyname: string;
  represent: string;
  committee: number;
  billtitle: string;
  progressbar: string[];
  panelsectiondata: string[]; // ProgressBar랑 이거는 어떤식으로 데이터 올지 모름. 추후 수정필요.
}
