import React from 'react';

export interface ProgressBarProps {
  step: number;
  date: any[];
  partycolor: string;
  status: string;
  finalDate: string;
  finalStatus: string;
  // children: React.ReactNode;
}
